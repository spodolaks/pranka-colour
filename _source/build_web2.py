# SUPERSEDED - do not use.
#
# This script only ever built part of the page and resolved the masks frames to
# the low-resolution copies in the flat retouched/ folder rather than the
# full-size masters in retouched/masks/. It also predates the current slider
# geometry.
#
# Use build_all.py instead. It builds every asset the page needs and resolves
# masks/, iceland/ and 'the endless search'/ ahead of the flat folder.
